name: Android CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '8.14'

    - name: Install Web Dependencies
      run: npm install

    - name: Build Web App
      run: npm run build

    - name: Generate Native Icons
      run: npx @capacitor/assets generate --assetPath public/assets --iconBackgroundColor '#050508' --iconBackgroundColorDark '#050508' --splashBackgroundColor '#050508' --splashBackgroundColorDark '#050508' || echo "Asset generation skipped or failed, continuing build..."

    - name: Sync Capacitor
      run: npx cap sync android

    - name: Aggressively Clean Corruption
      run: |
        rm -rf ~/.gradle/caches/
        rm -f android/gradle/wrapper/gradle-wrapper.jar

    - name: Build Android APK with System Gradle
      working-directory: android
      run: gradle assembleDebug --no-daemon --refresh-dependencies

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: loki-prime-x-debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
