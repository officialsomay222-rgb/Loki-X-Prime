name: Android CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: # Allows you to run this workflow manually from the Actions tab

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24' # Using latest to remove warnings
        cache: 'npm'

    - name: Check Directory Structure
      run: |
        echo "Current directory content:"
        ls -la
        if [ -d "android" ]; then
          echo "Android folder found!"
        else
          echo "ERROR: Android folder NOT found. Please push the 'android' folder to GitHub."
          exit 1
        fi

    - name: Install Web Dependencies
      run: npm install

    - name: Build Web App
      run: npm run build

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Sync Capacitor
      run: npx cap sync android

    - name: Build Android APK
      working-directory: android
      run: gradle assembleDebug # Using 'gradle' directly instead of './gradlew' to bypass corrupt wrapper

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: loki-prime-x-debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
